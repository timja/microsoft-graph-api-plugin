<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.jenkins-ci.plugins</groupId>
    <artifactId>plugin</artifactId>
    <version>6.2189.v695a_c41f5249</version>
    <relativePath />
  </parent>

  <groupId>io.jenkins.plugins</groupId>
  <artifactId>microsoft-graph-api</artifactId>
  <version>${revision}-${changelist}</version>
  <packaging>hpi</packaging>

  <name>Microsoft Graph Entra API Plugin</name>
  <description>Provides a minimal, build-time-generated Microsoft Graph (Entra ID) client and the Microsoft Kiota runtime to Jenkins plugins.</description>
  <url>https://github.com/jenkinsci/${project.artifactId}-plugin</url>
  <licenses>
    <license>
      <name>MIT License</name>
      <url>https://opensource.org/license/mit/</url>
    </license>
  </licenses>
  <scm child.scm.connection.inherit.append.path="false" child.scm.developerConnection.inherit.append.path="false" child.scm.url.inherit.append.path="false">
    <connection>scm:git:https://github.com/${gitHubRepo}</connection>
    <developerConnection>scm:git:https://github.com/${gitHubRepo}</developerConnection>
    <tag>${scmTag}</tag>
    <url>https://github.com/${gitHubRepo}</url>
  </scm>

  <properties>
    <!-- The revision and graph.metadata.sha properties are updated together by
         .github/workflows/update-graph-metadata.yaml.
         revision = 1.<metadata build number> from the msgraph-metadata commit message
         "Update clean v1.0 metadata file with NNNNNN" -->
    <revision>1.225181</revision>
    <!-- Commit SHA of microsoftgraph/msgraph-metadata providing openapi/v1.0/openapi.yaml -->
    <graph.metadata.sha>70d1b3f19bda2495b22b6900354b030c260601d5</graph.metadata.sha>
    <changelist>999999-SNAPSHOT</changelist>

    <!-- Kiota CLI (code generator), see https://github.com/microsoft/kiota/releases -->
    <kiota.cli.version>1.32.5</kiota.cli.version>
    <!-- Microsoft Kiota Java runtime; keep >= the version pulled transitively by microsoft-graph-core -->
    <kiota.java.version>1.9.3</kiota.java.version>

    <!-- https://www.jenkins.io/doc/developer/plugin-development/choosing-jenkins-baseline/ -->
    <jenkins.baseline>2.541</jenkins.baseline>
    <jenkins.version>${jenkins.baseline}.3</jenkins.version>
    <gitHubRepo>jenkinsci/${project.artifactId}-plugin</gitHubRepo>

    <spotless.check.skip>false</spotless.check.skip>
    <ban-junit4-imports.skip>false</ban-junit4-imports.skip>
    <ban-commons-lang-2.skip>false</ban-commons-lang-2.skip>
    <ban-deprecated-stapler.skip>false</ban-deprecated-stapler.skip>

    <!-- Nearly all main code is generated by Kiota; SpotBugs findings there are not actionable -->
    <spotbugs.skip>true</spotbugs.skip>
    <hpi.bundledArtifacts>jakarta.annotation-api,microsoft-graph-core,microsoft-kiota-abstractions,microsoft-kiota-authentication-azure,microsoft-kiota-http-okHttp,microsoft-kiota-serialization-form,microsoft-kiota-serialization-json,microsoft-kiota-serialization-multipart,microsoft-kiota-serialization-text,std-uritemplate</hpi.bundledArtifacts>
    <hpi.strictBundledArtifacts>true</hpi.strictBundledArtifacts>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>io.jenkins.tools.bom</groupId>
        <artifactId>bom-${jenkins.baseline}.x</artifactId>
        <version>6667.v64d86185fd17</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>

    <!-- Bundled: Graph core (AzureIdentityAuthenticationProvider, GraphClientFactory, middleware) -->
    <dependency>
      <groupId>com.microsoft.graph</groupId>
      <artifactId>microsoft-graph-core</artifactId>
      <version>3.6.6</version>
      <exclusions>
        <exclusion>
          <groupId>com.auth0</groupId>
          <artifactId>jwks-rsa</artifactId>
        </exclusion>
        <!-- from azure-sdk plugin -->
        <exclusion>
          <groupId>com.azure</groupId>
          <artifactId>azure-core</artifactId>
        </exclusion>
        <!-- from gson-api plugin -->
        <exclusion>
          <groupId>com.google.code.gson</groupId>
          <artifactId>gson</artifactId>
        </exclusion>
        <!-- from okhttp-api plugin -->
        <exclusion>
          <groupId>com.squareup.okhttp3</groupId>
          <artifactId>okhttp</artifactId>
        </exclusion>
        <!-- Only used for change-notification token validation, which is not exposed here -->
        <exclusion>
          <groupId>io.jsonwebtoken</groupId>
          <artifactId>*</artifactId>
        </exclusion>
        <!-- from opentelemetry-api plugin -->
        <exclusion>
          <groupId>io.opentelemetry</groupId>
          <artifactId>*</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <!-- Kiota runtime pieces referenced directly by the generated client class.
         They are only runtime-scope transitives of microsoft-graph-core, so they
         must be declared at compile scope for the generated sources to compile. -->
    <dependency>
      <groupId>com.microsoft.kiota</groupId>
      <artifactId>microsoft-kiota-abstractions</artifactId>
      <version>${kiota.java.version}</version>
      <exclusions>
        <exclusion>
          <groupId>io.opentelemetry</groupId>
          <artifactId>*</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>com.microsoft.kiota</groupId>
      <artifactId>microsoft-kiota-authentication-azure</artifactId>
      <version>${kiota.java.version}</version>
      <exclusions>
        <exclusion>
          <groupId>com.azure</groupId>
          <artifactId>azure-core</artifactId>
        </exclusion>
        <exclusion>
          <groupId>io.opentelemetry</groupId>
          <artifactId>*</artifactId>
        </exclusion>
        <!-- part of the JDK (java.xml) -->
        <exclusion>
          <groupId>javax.xml.stream</groupId>
          <artifactId>stax-api</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>com.microsoft.kiota</groupId>
      <artifactId>microsoft-kiota-http-okHttp</artifactId>
      <version>${kiota.java.version}</version>
      <exclusions>
        <exclusion>
          <groupId>com.squareup.okhttp3</groupId>
          <artifactId>okhttp</artifactId>
        </exclusion>
        <exclusion>
          <groupId>io.opentelemetry</groupId>
          <artifactId>*</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>com.microsoft.kiota</groupId>
      <artifactId>microsoft-kiota-serialization-form</artifactId>
      <version>${kiota.java.version}</version>
    </dependency>
    <dependency>
      <groupId>com.microsoft.kiota</groupId>
      <artifactId>microsoft-kiota-serialization-json</artifactId>
      <version>${kiota.java.version}</version>
      <exclusions>
        <exclusion>
          <groupId>com.google.code.gson</groupId>
          <artifactId>gson</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>com.microsoft.kiota</groupId>
      <artifactId>microsoft-kiota-serialization-multipart</artifactId>
      <version>${kiota.java.version}</version>
    </dependency>
    <dependency>
      <groupId>com.microsoft.kiota</groupId>
      <artifactId>microsoft-kiota-serialization-text</artifactId>
      <version>${kiota.java.version}</version>
    </dependency>
    <!-- Jenkins API plugins supplying shared libraries at runtime -->
    <dependency>
      <groupId>io.jenkins.plugins</groupId>
      <!-- azure-core, azure-identity -->
      <artifactId>azure-sdk</artifactId>
    </dependency>
    <dependency>
      <groupId>io.jenkins.plugins</groupId>
      <artifactId>gson-api</artifactId>
    </dependency>
    <dependency>
      <groupId>io.jenkins.plugins</groupId>
      <artifactId>okhttp-api</artifactId>
    </dependency>
    <dependency>
      <groupId>io.jenkins.plugins</groupId>
      <artifactId>opentelemetry-api</artifactId>
    </dependency>
    <!-- @jakarta.annotation.Nonnull/@Nullable used throughout generated code;
         runtime-scope transitive of the kiota artifacts, needed at compile scope -->
    <dependency>
      <groupId>jakarta.annotation</groupId>
      <artifactId>jakarta.annotation-api</artifactId>
      <version>2.1.1</version>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.wiremock</groupId>
      <artifactId>wiremock-standalone</artifactId>
      <version>3.13.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <repositories>
    <repository>
      <id>repo.jenkins-ci.org</id>
      <url>https://repo.jenkins-ci.org/public/</url>
    </repository>
  </repositories>
  <pluginRepositories>
    <pluginRepository>
      <id>repo.jenkins-ci.org</id>
      <url>https://repo.jenkins-ci.org/public/</url>
    </pluginRepository>
  </pluginRepositories>

  <build>
    <plugins>
      <plugin>
        <groupId>io.kiota</groupId>
        <artifactId>kiota-maven-plugin</artifactId>
        <version>0.0.36</version>
        <configuration>
          <kiotaVersion>${kiota.cli.version}</kiotaVersion>
          <!-- default is 30s; the ~37MB Graph spec takes far longer to process -->
          <kiotaTimeout>1800</kiotaTimeout>
          <url>https://raw.githubusercontent.com/microsoftgraph/msgraph-metadata/${graph.metadata.sha}/openapi/v1.0/openapi.yaml</url>
          <!-- SHA in the path defeats the "skip download if file exists" cache on bumps -->
          <downloadTarget>${project.build.directory}/openapi-spec/${graph.metadata.sha}</downloadTarget>
          <language>Java</language>
          <namespace>io.jenkins.plugins.microsoftgraph</namespace>
          <!-- A hand-written GraphServiceClient subclass adds the constructors and
               getRequestAdapter() accessor the official SDK's client exposes -->
          <clientClass>BaseGraphServiceClient</clientClass>
          <excludeBackwardCompatible>true</excludeBackwardCompatible>
          <cleanOutput>true</cleanOutput>
          <includePath>
            <path>/users#GET</path>
            <path>/users/{user-id}#GET</path>
            <path>/users/{user-id}/transitiveMemberOf#GET</path>
            <path>/users/{user-id}/photos/{profilePhoto-id}#GET</path>
            <path>/users/{user-id}/photos/{profilePhoto-id}/$value#GET</path>
            <path>/groups#GET</path>
            <path>/groups/{group-id}#GET</path>
          </includePath>
          <!-- Microsoft factories, NOT the io.kiota Jackson defaults of this Maven plugin,
               so the generated client is compatible with microsoft-graph-core -->
          <serializers>
            <serializer>com.microsoft.kiota.serialization.JsonSerializationWriterFactory</serializer>
            <serializer>com.microsoft.kiota.serialization.TextSerializationWriterFactory</serializer>
            <serializer>com.microsoft.kiota.serialization.FormSerializationWriterFactory</serializer>
            <serializer>com.microsoft.kiota.serialization.MultipartSerializationWriterFactory</serializer>
          </serializers>
          <deserializers>
            <deserializer>com.microsoft.kiota.serialization.JsonParseNodeFactory</deserializer>
            <deserializer>com.microsoft.kiota.serialization.TextParseNodeFactory</deserializer>
            <deserializer>com.microsoft.kiota.serialization.FormParseNodeFactory</deserializer>
          </deserializers>
          <disableValidationRules>
            <rule>All</rule>
          </disableValidationRules>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>generate</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
